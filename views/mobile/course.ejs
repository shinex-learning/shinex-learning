<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=yes">
    <title><%= course.title %> – SHINEX</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div id="page-loader">
        <div class="loader-logo"><span>SLC</span><div class="loader-water"></div></div>
        <div class="loader-text">SHINEX</div>
        <div class="loader-sub">LEARNING CIRCLE</div>
    </div>

    <%- include('../partials/header-mobile') %>

    <main class="main-content" style="padding:12px 0 24px;">
        <div class="container" style="padding:0 12px;">
            <% if (messages && messages.error) { %>
                <div class="alert alert-danger flash-message" data-type="error"><%= messages.error %></div>
            <% } %>
            <% if (messages && messages.success) { %>
                <div class="alert alert-success flash-message" data-type="success"><%= messages.success %></div>
            <% } %>

            <div style="background:#fff; border-radius:12px; padding:16px; border:1px solid #ede8f5; margin-bottom:12px;">
                <h1 style="font-size:1.2rem; font-weight:800; color:var(--text-dark); margin:0;"><%= course.title %></h1>
                <div style="color:var(--text-muted); font-size:0.85rem; margin:4px 0 6px;"><%= course.description %></div>
                <div style="display:flex; flex-wrap:wrap; gap:6px;">
                    <span class="badge badge-purple" style="font-size:0.65rem;"><%= course.level || 'Beginner' %></span>
                    <span class="badge badge-dark" style="font-size:0.65rem;"><i class="far fa-clock"></i> <%= course.duration %></span>
                    <span class="badge badge-dark" style="font-size:0.65rem;"><i class="fas fa-layer-group"></i> <%= course.levels ? course.levels.length : 0 %> Levels</span>
                </div>
            </div>

            <div style="font-weight:700; font-size:0.95rem; color:var(--text-dark); margin:0 0 6px;">
                <i class="fas fa-layer-group" style="color:var(--primary);"></i> Levels
            </div>

            <% if (course.levels && course.levels.length > 0) { %>
                <div style="display:flex; flex-direction:column; gap:10px;">
                    <% course.levels.forEach(function(level) { %>
                        <div style="background:#f8f6fc; border-radius:10px; padding:14px 16px; border:1px solid #ede8f5; text-align:center;">
                            <div style="font-weight:700; font-size:1rem; color:var(--text-dark);"><%= level.name %></div>
                            <div style="font-size:0.8rem; color:var(--text-muted);"><i class="far fa-clock"></i> <%= level.duration %></div>
                            <div style="font-size:0.75rem; color:var(--text-muted); margin:4px 0 8px;">
                                <i class="fas fa-book"></i> 
                                <% let lc = 0, cc = 0; if (level.lessons) { level.lessons.forEach(function(l) { lc++; if (l.classes) cc += l.classes.length; }); } %>
                                <%= lc %> lessons • <%= cc %> classes
                            </div>
                            <a href="/level/<%= course.id %>/<%= level.id %>" class="btn btn-primary btn-sm" style="width:100%; font-size:0.8rem; padding:8px;">Start</a>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <p style="color:var(--text-muted); text-align:center; padding:16px 0;">No levels available.</p>
            <% } %>

        </div>
    </main>

    <%- include('../partials/footer-mobile') %>

    <script src="/js/script.js"></script>
</body>
</html>